/**
 * constants/routes.js
 * Centralised route path map.
 * Import ROUTES everywhere instead of hardcoding path strings.
 */

export const ROUTES = {
  HOME: '/',
  DASHBOARD: '/dashboard',
  NOT_FOUND: '*',
};
